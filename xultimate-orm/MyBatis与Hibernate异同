不同点
	没有值类型和实体类型的概念。
		无法处理继承。
		不存在持久化生命周期概念，因此没有瞬时状态、托管状态、持久状态、删除状态。
		没有持久化上下文，因此不存在对象同一性和对象等同性。
		不支持API级别的乐观锁和悲观锁。
		不支持传播性持久化。
		自动生成SQL语句。
		不支持HQL。
		Hibernate主要是以面向对象的方式来使用。MyBatis体现的是对JDBC的封装。
	没有监听器的概念。
相同点
	都支持定制类型处理器。
	都包含了级联映射。
	支持抓取计划（延迟加载和立即加载）
	支持外部缓存。
	都支持SQL。